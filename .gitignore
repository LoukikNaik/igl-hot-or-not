# votes / user data — regenerated at runtime, not source
db.json

# temp media from the transcription pipeline
transcripts/*.m4a
transcripts/*.mp4

# os / editor noise
.DS_Store
*.log
node_modules/

# local agent config
.claude/
backups/
db.json.bak
votes.sqlite
votes.sqlite-shm
votes.sqlite-wal
*.migrated
transcripts/*.part
transcripts/*.ytdl
transcripts/*.part-*
logs/
blocked-ips.txt
